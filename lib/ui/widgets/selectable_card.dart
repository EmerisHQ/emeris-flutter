import 'package:flutter/material.dart';

class SelectableCard extends StatelessWidget {
  const SelectableCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
