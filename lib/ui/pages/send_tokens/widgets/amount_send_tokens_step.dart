import 'package:flutter/material.dart';

class AmountSendTokensStep extends StatelessWidget {
  const AmountSendTokensStep({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
    );
  }
}
