import 'package:flutter/material.dart';

class ReviewSendTokensStep extends StatelessWidget {
  const ReviewSendTokensStep({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
